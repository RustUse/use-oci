# Releasing

`use-oci` follows the RustUse lockstep first-publish pattern.

Do not publish from this scaffold directly. The first public wave should dry-run focused crates first, wait for crates.io propagation, then dry-run and publish the facade crate last.
